export const SignUpIconWrapper = ({children, className}:{children:any; className:string}) => (
    <div style={{borderRadius:"50%"}} className={`${className} flex items-center rounded-small justify-center`}>
      {children}
    </div>
);